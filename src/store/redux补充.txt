1、store是唯一的
2、只有store能够改变自己的内容
3、Reducer必须是纯函数（给定固定的输入，就一定会有固定的输出，且不会有任何副作用）
4、
    createStore
    store.dispatch
    store.getState
    store.subscribe
5、无状态组件
const ** = (props)=>{
    return({

    })
}

